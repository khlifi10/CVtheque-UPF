{% extends 'base.html.twig' %}

{% block title %}Activites{% endblock %}

{% block body %}
    <h1>Activites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>IdAct</th>
                <td>{{ activite.IdAct }}</td>
            </tr>
            <tr>
                <th>ListeActivite</th>
                <td>{{ activite.ListeActivite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activites_index') }}">back to list</a>

    <a href="{{ path('activites_edit', {'id': activite.id}) }}">edit</a>

    {{ include('activites/_delete_form.html.twig') }}
{% endblock %}
